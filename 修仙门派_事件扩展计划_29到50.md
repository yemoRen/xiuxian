# 修仙门派 · 随机事件扩展计划（29 → 50 条）

> 草案 v1 ｜ 2026-09-12 ｜ **状态：待 yemo 确认后才动手**
> 涉及文件：`scripts/core/DataCore.gd`（事件表 `EVENTS`）、`scripts/core/GameCore.gd`（`apply_event`）

---

## 0. 先说两件已经办完的事

**① `xiuxian_sect/` 确认就是网页版，已清理**

| 项 | 结果 |
|---|---|
| 内容 | `index.html` / `data.js` / `game.js` / `ui.js` / `style.css`，5 个文件 96 KB |
| 依赖检查 | `xiuxian-godot/` 全目录 grep「xiuxian_sect」**零命中**，Godot 工程与两套测试都不依赖它 |
| 处理 | 5 个文件已进回收站；另归档压缩包 `.workbuddy/archive/xiuxian_sect_web_20260912.zip`（28 KB，可还原） |

**② 现有事件框架的硬约束（决定了这次"能怎么扩"）**

*触发模型*（`GameCore.random_events`）：

```
每旬（1 tick）→ 28% 概率触发 1 次
             → 从「在世弟子」中随机抽 1 人作为当事人
             → 气运之子：good 类权重 ×3　｜　魔修：demon 类权重 ×4
```

*效果模型*（`GameCore.apply_event`）—— **这是本次扩展的关键**：

> 效果不是按事件 id 分发的，而是把文案渲染完之后，**拿文案去做子串匹配**，猜这条事件该加什么。
> 例：`if t.contains("+2%"): p.qiRateB += 0.02`

现在能用的钩子一共 **12 个吉 + 6 个凶 + 2 个特殊**：

| 吉类（`kind="good"`，全部额外 +10 心情） | 凶类（`kind="bad"`，全部额外 -10 心情） |
|---|---|
| `+2%` → 修炼速度 +0.02 | `修为 -` → 当前修为 - 上限×20% |
| `+3%` → 修炼速度 +0.03 | `寿元 -` → 寿元 -20 |
| `+20%` → 修炼速度 +0.20 | `灵石 -` → 灵石 - 500~5000（**不带**境界倍率） |
| `+10%` → **武力** +0.10（注意是武力，不是修炼速度） | `突破几率 -` → 突破几率 -3% |
| `修为 +` → 当前修为 + 上限×30% | `心情 -` → 心情 **再** -20（叠通用 -10 → 共 -30） |
| `武力 +` → 武力 +0.10 | `法宝损坏` → 随机损坏一件已装备法宝 |
| `灵石 +` → 灵石 + 500~5000 ×(1+境界×2) | |
| `丹药 +` → 随机一味丹药 +1 | |
| `庇护人数` → 庇护人数 + 随机 20~200 | |
| `突破几率 +` → 突破几率 +5% | |
| `寿元 +` → 寿元 +30 | |

`kind="love"`：命中 `结为了道侣` 才真结婚（双方突破几率 +10%），另有固定 +15 心情。
`kind="demon"`：固定把当事人变成魔修（心情=20，突破几率 -5%），与文案无关。

**四个必须记住的坑**（上一轮已经踩过一遍）：

1. **`+10%` 是武力、`+2%/+3%/+20%` 是修炼速度** —— 语义不一致，所以「修炼速度 +10%」这种写法是**错的**，会变成加武力。
2. **一条文案会命中多个钩子**：「武力 +10%」同时命中 `+10%` 和 `武力 +` → 实际 +20%。
3. **`{a}` / `{s}` 是陷阱占位符**（显示的数值和实际施加的数值不是同一个东西），上一轮已停用。目前**只有 `{q}` 和 `{p}` 是"显示值 = 实际值"**。
4. **`心情 -` 会叠在通用 -10 上**，文案要写合计数。

---

## 1. 所以"加 21 条"不是简单堆文案

如果只往 `EVENTS` 里堆文字，会立刻撞到两堵墙：

- **墙一：效果只有 18 种组合。** 50 条事件挤在 18 个效果上，一半以上会变成"换个说法、效果一样"的换皮，玩家看得出来。
- **墙二：改文案随时改效果。** 每加一条都要人工确认"命中的子串集合和预期一致"，21 条就是 21 次踩雷机会 —— 这正是上一轮花整轮时间修的问题的成因。

结论：**要让 50 条真有 50 条的手感，必须先让"文案"和"效果"解耦。**

---

## 2. 三条技术路线

| | 路线 A：纯复用 | **路线 B：加 `eff` 字段（推荐）** | 路线 C：全量 id 化 |
|---|---|---|---|
| 做法 | 只加文案，效果靠现有 18 个钩子凑 | 给事件加可选字段 `eff`，`apply_event` 优先按 `eff` 施加；没 `eff` 的走原逻辑 | 50 条全改成 `id + eff`，删掉字面匹配 |
| 旧 29 条 | 不动 | **完全不动，行为逐位不变** | 全部要改 |
| 新效果 | 不能加 | 可加任意新效果 | 可加 |
| 文案自由度 | 极低（写错就改效果） | **完全自由** | 完全自由 |
| 改动面 | 0 行逻辑 | `apply_event` 加 1 个分支 + 1 个派发表（约 60 行） | 大（重写事件系统） |
| 风险 | 低但产出质量差 | **低**（旧路径原样保留） | 中 |
| 回归基线 | 可继续用 | **可继续用** | 基线需重建 |

**建议走路线 B。** 理由：新增的 21 条里有一半需要新效果才有意义，路线 B 恰好以最小代价打开这个空间，同时旧 29 条一条不改 —— 数学上不可能影响现有平衡。

---

## 3. 路线 B 的设计

### 3.1 数据结构

```gdscript
# 旧事件（保持原样，走字面匹配）
{"w": 5, "kind": "good", "t": "{n}斩一恶蛟，取其筋为束带。武力 +一成（即 10%）"}

# 新事件（带 eff，走结构化施加）
{"w": 5, "kind": "good", "t": "{n}在药园深处移栽回一株野生灵草……。灵草 +1（随机一种）",
 "eff": [["mood", 10], ["herb", 1]]}
```

### 3.2 `apply_event` 的改法

```
渲染文案（占位符替换，不变）
  ↓
if ev.has("eff"):
    按 ("eff" 逐项施加)     ← 新增分支
    push_log(...)
    return
else:
    走原来的字面匹配        ← 一行不动
```

### 3.3 一条重要约定：`eff` 模式**完全接管**

走 `eff` 的事件**不再**自动附加"good +10 心情 / bad -10 心情"，love 的固定 +15 也改为显式声明。

这样 **文案里写的每一条 = eff 里声明的每一条**，一一对应、可机器校验，从根上杜绝"文案与实际不符"。

---

## 4. 需要新增的效果钩子

`eff` 每一项是 `[键, 数值]`。数值的正负表示方向；随机幅度的效果（灵石/庇护人数）沿用现有的随机区间。

| 键 | 含义 | 施加方式 | 风险 |
|---|---|---|---|
| `qiRate` | 修炼速度 +n | `p.qiRateB += n` | 低 |
| `atk` | 武力加成 +n | `p.atkBonus += n` | 低 |
| `brk` | 突破几率 ±n | `p.brkBonus += n` | 低 |
| `qi` | 修为 ±上限×n | `p.qi = max(0, qi + qi_max×n)` | 低 |
| `mood` | 心情 ±n（**精确值**） | `clamp(p.mood + n, 0, 100)` | 低 |
| `lifespan` | 寿元 ±n | `p.lifespanBonus += n` | 低 |
| `stone` | v>0 → 灵石 +一笔（含境界倍率）；v<0 → 灵石 -一笔（不含） | 与现有吉/凶事件同式 | 中（经济） |
| `pill` | ±n 味**随机**丹药 | `s.pills[k] += n`（下限 0） | 低 |
| `herb` | ±n 种**随机**灵草 | `s.herbs[i] += n`（下限 0） | 中（经济） |
| `ore` | ±n 种**随机**矿石 | `s.ores[i] += n`（下限 0） | 中（经济） |
| `equips` | 得 n 件法宝 | `make_equip(按境界保守取 1~4 级)` | 中（战力） |
| `protect` | 庇护人数 ±随机 20~200 | `s.protect = max(0, …)` | 低 |
| `merit` | 宗门功绩 ±n | `s.merit += n` | 低（暂无消耗方） |
| `marry` | 结为道侣（含双方突破 +10%） | 复用现有逻辑 | 低 |
| `breakEquip` | 损坏一件法宝 | 复用现有逻辑 | 低 |
| `demonize` | 堕为魔修 | 复用现有逻辑 | 低 |

> 说明：新增的都是"施加到现有状态字段"的操作，**没有引入新的状态字段、没有改 tick 循环**，因此不会打乱主循环结构。

---

## 5. 21 条新事件草案

新增分布：**吉 +8、凶 +6、平常 +5、情缘 +2**（合计 50 条）

### 5.1 吉（+8）—— 带 eff 的结构化事件

| # | 文案 | eff | 权重 |
|---|---|---|---|
| 1 | {n}依古法采得灵草，于丹炉前守了三日三夜，终成一炉好药。丹药 +1（随机一味） | `mood 10`、`pill 1` | 5 |
| 2 | {n}在药园深处移栽回一株野生灵草，悉心照料，长势喜人。灵草 +1（随机一种） | `mood 10`、`herb 1` | 5 |
| 3 | {n}替宗门跑了一趟坊市，低买高卖，颇有进项。灵石 +一笔（数目随缘） | `mood 10`、`stone +1` | 4 |
| 4 | {n}于后山采石，意外凿出一块蕴灵矿石。矿石 +1（随机一种） | `mood 10`、`ore 1` | 4 |
| 5 | {n}与同门论道三日，解开了多年积郁，心境澄明。心情 +30 | `mood 30` | 4 |
| 6 | {n}夜观天象，悟得一线天机，气机大畅。修炼速度 +5% | `mood 10`、`qiRate 0.05` | 3 |
| 7 | {n}于一场拍卖会上捡漏，得了一件趁手法宝。法宝 +1 | `mood 10`、`equips 1` | 3 |
| 8 | {n}雷雨夜独登绝顶，硬撼一道天雷而不倒。武力 +15% | `mood 15`、`atk 0.15` | 3 |

> #5 / #8 是之前做不到的效果：**心情 +30**（原来只有通用的 +10）和**武力 +15%**（原来只有 10%/20% 档）。#2 / #4 / #7 是全新的资源产出（灵草 / 矿石 / 法宝）。

### 5.2 凶（+6）

| # | 文案 | eff | 权重 |
|---|---|---|---|
| 1 | {n}的丹炉炸了膛，一炉丹药尽毁。丹药 -1（随机一味） | `mood -10`、`pill -1` | 4 |
| 2 | {n}打理药园时失手，毁了半圃灵草。灵草 -1（随机一种） | `mood -10`、`herb -1` | 4 |
| 3 | {n}在坊市被人做局，折了一大笔灵石。灵石 -一笔（数目随缘） | `mood -10`、`stone -1` | 4 |
| 4 | {n}强行冲击瓶颈，气血翻涌，险些走火入魔。突破几率 -5% | `mood -10`、`brk -0.05` | 3 |
| 5 | {n}所修功法出了纰漏，丹田受损。修为 -20%（按上限计） | `mood -10`、`qi -0.2` | 3 |
| 6 | {n}在荒野遇上一伙劫修，苦战脱身。心情 -30 | `mood -30` | 3 |

> 凶类新增了"**反向的丹药 / 灵草 / 阵法**"损失，让资源不是只增不减。

### 5.3 平常（+5）—— 纯叙事，无效果

| # | 文案 | 权重 |
|---|---|---|
| 1 | {n}在宗门山门外看了一整日的云，什么也没做。 | 7 |
| 2 | {n}与山下货郎闲聊半日，听了一肚子外面的事。 | 6 |
| 3 | {n}把旧年的符纸翻出来晒了晒，顺手烧掉几张。 | 6 |
| 4 | {n}夜里睡不着，独自在演武场数了一夜星星。 | 5 |
| 5 | {n}试着养了一盆灵植，第三日就枯了。 | 5 |

> 平常类本来就是"世界的呼吸"，不加效果、零风险，但能显著稀释重复感 —— 这是**性价比最高的一批**。

### 5.4 情缘（+2）

| # | 文案 | eff | 权重 |
|---|---|---|---|
| 1 | {n}与{m}一同下山采买，归途遇雨共避一檐，相顾无言，耳根却红了。 | `mood 15` | 5 |
| 2 | {n}与{m}历经波折终成眷属，同修之路再不孤单。 | `marry`、`mood 15` | 3 |

> 原 3 条情缘里只有 1 条真结婚，另 2 条是纯文本。#2 让"结为道侣"这件事多一个入口（效果与现有 `结为了道侣` 一致）。

---

## 6. 扩展后的权重与概率

新增权重合计 89（吉 31 / 凶 21 / 平常 29 / 情缘 8）。

| 类别 | 条数 | 权重 | 触发占比 | 对比现在 |
|---|---|---|---|---|
| 吉 | 20 | 75 | **36.6%** | 38.6% → 略降 |
| 平常 | 11 | 62 | **30.2%** | 28.9% → 略升 |
| 凶 | 13 | 48 | **23.4%** | 23.7% → 持平 |
| 情缘 | 5 | 16 | **7.8%** | 7.0% → 略升 |
| 堕魔 | 1 | 4 | **2.0%** | 1.8% → 持平 |
| **合计** | **50** | **205** | 100% | |

> 堕魔类只有 1 条文案，但权重从 2 提到 4 —— **多的是触发次数，不是花样**。若你觉得堕魔该更罕见，改回 2 即可（占比降到 1.0%）。
>
> 特殊修正后的实际观感：气运之子身上吉事件占 **63.4%**；魔修身上堕魔事件占 **7.4%**。

**这是本次唯一需要你定的数值**，其余都是可控的。

---

## 7. 执行步骤与验收

| 步骤 | 内容 | 验收 |
|---|---|---|
| 1 | `DataCore.gd` 加 21 条事件（带 `eff`） | 事件总数 50，字段结构合法 |
| 2 | `GameCore.gd` 加 `eff` 派发表 + `apply_event` 分支（约 60 行） | 旧路径零改动（`git diff` 可核对） |
| 3 | 新增 `scripts/test/EffTest.gd` | 每个 eff 键至少 1 条单测，逐项比对状态增量 |
| 4 | 扩展 `build.py` 总表：加「效果来源」（legacy/eff）+「效果(结构化)」列 | 50 条事件的**文案声明**与**eff 声明**自动比对，**必须 0 处不符** |
| 5 | 跑 `LogicTest` / `UISmoke` | 112/112、108/108 全绿（旧事件未动，应当零差异） |
| 6 | 跑 4000 旬长跑 sanity | 不崩、灵石不枯竭、飞升数与天梯进度落在合理区间 |
| 7 | 重算总表公式 + 交叉校验 | 29+21 = 50 条与源文件逐条一致 |

**关于第 6 步的预期变化**：新增事件会真的触发，所以长跑的随机数流会变、结果**不会**和现在逐位相同 —— 这是预期的。要看的是**有没有跑偏**（尤其是 `herb` / `ore` / `equips` 三个新产出会不会让灵石通胀）。

---

## 8. 需要你拍板的 4 件事

1. **走哪条路线？** 我建议**路线 B**（加 `eff` 字段，旧事件零改动）。
2. **21 条草案要不要调？** 文案口味、事件题材、数值档位都可以改 —— 这只是草案。
3. **权重表要不要改？** 默认方案是"保持现有手感，仅把堕魔权重从 2 提到 4"。你也可以指定"吉多一点 / 凶狠一点"。
4. **触发率 28% 动不动？** 50 条只是**减少重复**，不会增加触发次数。如果你希望"宗门里更热闹"，可以调高这个值（例如 35%）—— 但**这会直接改变平衡**，需要单独跑长跑确认。

---

## 附：遗留项

`xiuxian_sect` 目录**已清空**（5 个文件进回收站 + 已归档），但目录本身被某个进程占着句柄（Windows `ERROR_SHARING_VIOLATION`，err=32），暂时删不掉。重启 WorkBuddy 后即可直接删除，或在资源管理器里手动删（就是个空文件夹）。

---

## 9. 追加：落地后的稀释补偿（2026-09-12 晚）

扩展上线后发现 4000 旬长跑的飞升次数从 7 降到 4（天梯 7/9 → 4/9）。做了完整的 A/B 归因（详见 `事件扩展AB归因报告.html`），结论：

- **「7 → 4」是单种子假象**：原池 400 种子均值 4.24，种子 20260912 的 7 次属上分位幸运路径；线上池均值 3.79。
- **真实效应 −10.7%（p≈0.0003）**，不是腰斩；上限（最高境界恒为 8）与经济（灵石、门徒规模）均未变。
- **成因＝稀释**：事件池总权重 114 → 205，把「推进境界」这类有效吉事件的密度从 18.1% 摊薄到 11.7%。单独加 5 条「平常」或 16 条 eff 都测不出，合起来才显形（交互效应）。
- **brk（突破加成）假说已证伪**：三个定向诊断臂全部仍显著偏低。
- **修复方向＝补分子，不是压分母**：把新增事件权重减半反而更差（−15%），因为新 eff 事件本身净正向。

### 已执行的改动（本节即落地记录）

| 事件 | 原权重 | 新权重 |
|---|---|---|
| {n}于山涧拾得一枚无名种子…修炼速度 +2% | 6 | **9** |
| {n}在旧书堆中翻出一卷残篇…修炼速度 +3% | 6 | **9** |
| {n}夜观星象，忽有所悟…修为 +{q} | 5 | **8** |
| {n}于雷劫中窥见一线生机…突破几率 +5% | 2 | **3** |
| {n}梦中得仙人授法…修炼速度 +20% | 2 | **3** |

- 总权重 **205 → 216**；**只改权重数字**，文案、效果、代码零改动。
- 同步落点：`DataCore.gd`（运行时）+ `build.py` 的 `EVENTS`（总表事实源），`CROSSCHECK PASS`。
- **落地后复验（400 离散种子）**：线上池 4.03 vs 原池 4.24，差值 −0.21，**p≈0.0985（不显著）** —— 恢复到统计噪声内。
- 回归：EffTest 63/63 ｜ LogicTest 112/112 ｜ AlignmentTest 45/45 ｜ UISmoke 108/108。
  - 注：`LogicTest` 的「丹药数量保留」原为硬编码常量（会被 200 旬随机事件改动），已改为在存档时刻动态取值后再比对，语义不变、不再随事件权重漂移而误报。
