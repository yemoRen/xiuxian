# 更新日志

本项目的版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)：
`主版本号.次版本号.修订号` —— 不兼容的改动升主版本，向下兼容的新功能升次版本，问题修复升修订号。

---

## [1.1.5] - 2026-09-18

### 新增

- **法宝公库分页**：道具页法宝公库改为**每页 15 件**，底部显示「第 X / Y 页 · 共 Z 件」，处于首页 / 末页时自动禁用上 / 下页按钮。
- **法宝部位筛选**：公库新增「全部 / 发冠 / 衣甲 / 鞋履 / 佩饰 / 武器」六个筛选按钮，选中态高亮；点击任意筛选项会自动回到第 1 页。
- **法宝品阶降序**：公库列表默认按**品阶从高到低**排列（昊苍阶在最前），同阶按名称升序 —— 一眼挑出当前最好的那件。
- **穿戴弹窗补全属性**：角色详情 →「法宝栏」→ 点击部位弹出的穿戴选择窗，每件法宝现在显示**名称 · 器灵**、**品阶 · 部位**，以及逐条**属性加成**（如 修炼 +12%、武力 +8%）；无附加属性时明确标注「无附加属性」。

### 调整

- **除魔战利品编制权重下调**：副手 **50% → 30%**，与进度效率 `PEAK_W_*`（峰主 100% / 副手 30% / 外门 10%）数值同构。满编（1 峰主 + 4 副手 + 30 外门）战利品权重由 **6.0 降为 5.2**，八档庇护合计由 9,702 降为 **8,408**（约 −13%）；攻破周期与秘境产出不受影响。

### 优化

- `index.pck` 由 7.54 MB 变为 **7.57 MB**（+0.031 MB / +32 KB）：本轮无新增美术素材。

### 回归

- UISmoke 新增公库分页 / 筛选 / 降序 / 穿戴弹窗属性断言，全套探针全绿（LogicTest 154/0、UISmoke 328/0、_V115Probe 38/0、_V114Probe 24/0）。

---

## [1.1.4] - 2026-09-18

### 新增

- **除魔「惨胜」**：除魔陨落改为**每次结算独立掷骰**（与成败无关）—— 赢了也可能折损人手。出现陨落时全队按 **70% 损耗**结算（取较大值、**不叠乘**），战利品照发，并推送专属纪事「除魔队伍X攻破Y，魔气虽清，Z却战殁于此，余众亦元气大伤。」
- **除魔陨落曲线重做**：`风险 ≥ 0.2` 起判定，线性升至风险 0.95 时 **80%**（旧版仅在高风险且讨伐失败时以 `(风险−0.7)×0.1` 判定，每次实际最高 2.4%）。陨落池仍为**外门**，峰主 / 副手只会负伤减寿。
- **除魔失败补「负伤减寿」**：与秘境后果统一 —— 讨伐失败时可能有人负伤（寿元 −1~5 年 + 30% 伤势），纪事记「在**除魔征战**中负伤」。
- **进度条下方新增「消耗 + 效率」行**：丹宸 / 玄铸 / 青芜 / 玄矿 / 撰书阁五峰，进度条下方显示「每枚（件）耗料 …」与右侧金色实时效率（炼丹效率 / 炼器效率 / 种植效率 / 采矿效率 / 撰写速度）。

### 调整

- **除魔产出改「加权编制」**：峰主 **100%** / 副手 **50%** / 外门 **10%**。满编（1 峰主 + 4 副手 + 30 外门）产出约 **−80%**；裸峰主不再零产出（得 800 灵石 / 30 庇护）。
- **攻破 / 攻略周期随难度指数上升**：进度每旬 `×= 0.8^难度`（原与难度无关）。除魔 **2.0 → 16.0 年**（难度 0→9）、秘境 **2.4 → 16.0 年**（难度 0→8）；**奖励仍随难度递增**。镇邪 / 寻幽面板信息行新增「周期约 X」。
- **除魔庇护与武力倍率解耦（前平后陡）**：庇护改按除魔地档位查表 `1 / 1.4 / 2 / 3 / 4.5 / 7 / 12.5 / 22.5`（满编 180 / 252 / 360 / 540 / 810 / 1,260 / 2,250 / 4,050），八档合计 **−38%**。同时修掉「宗门周边因无推荐武力被强制 1 倍、落月城起却吃到约 2.25 倍」造成的一档 4.5 倍断崖。灵石仍受武力倍率影响。
- **每旬香火下调**：受庇护民众的香火供奉由 `庇护/500` 降为 `庇护/2000`。
- **杂役转化放缓**：`庇护/15` → `庇护/150`（攒满 300 人需约 4.5 万庇护，原约 1.5 万）。
- **资源峰杂役加成统一为 +1% / 人**：删去资源峰专属的绝对值加成，与除魔 / 秘境峰一致（杂役只放大已有队伍）。
- **角色详情**：「探索速度」→「探索效率」、「炼丹速度」→「炼丹效率」。

### 修复

- **藏经楼写作加成实际一直是 6% / 级**：v1.1.3 只改了配置表（2%），`GameCore` 中写作速度与单卷强度两处硬编码 0.06 未同步 —— 本次一并改为 0.02。

### 界面

- 功法撰写阁文案改为「根据功法灵根占比全宗获益」，并显示撰写速度（0 级藏经楼 = 1.00）。

### 优化

- `index.pck` 由 7.52 MB 变为 **7.54 MB**（+约 17 KB）：本轮无新增美术素材。

### 文档

- 新增 `数值参考_除魔秘境与杂役.md`：除魔地奖励详表、攻破周期表、陨落率两张对照表、惨胜收益阶梯、编制对比（1 峰主 vs 满编）。

---

## [1.1.3] - 2026-09-18

### 新增

- **逐出宗门**：角色详情面板底部新增「逐出宗门」按钮（红色危险样式），二次确认后解除其职务、师徒、道侣关系并移出名册（掌门不可逐）。与被逐者有正向关系的道侣 / 师徒 / 亲缘在世者心情 −20，并逐人记入纪事。
- **灵石月凝**：宗门每月自然凝聚灵石 100 枚；**聚灵阵正式实装**——每级每月额外 +20 枚，上限 50 级（此前「聚灵阵」是无结算点的摆设条目，本轮起真实生效）。
- **建宗公库自带一件荒阶法宝**：新局公库初始放入一件随机荒阶法宝，保证首次宗门大比（约 3 年后）必有奖品可发。
- **解除师徒**：角色详情面板「收徒」旁新增「解除师徒」按钮——弹窗选择要解除的徒弟并二次确认；被解除的徒弟心情 −20，纪事记「被 XX 解除师徒关系」。

### 调整

- **宗门大比到点自动暂停一次**：大比倒计时归零时自动暂停时间并飘字提醒；可点顶栏「宗门大比」主持，或点「推进」继续时间流逝。
- **收徒 / 拜师限内门**：只有内门弟子才能收徒，拜师也只能拜内门弟子——外门详情面板不再显示「收徒」，拜师候选仅列内门。
- **已有师尊不可另拜**：已拜师者不能再拜他人为师；宗门大比外门胜出者若已有师尊，择师尊页直接显示「XX 已在 XX 门下，师徒名分不变」，仅一个「确定」按钮。
- **徒弟不可解除师徒**：解除入口移到师尊侧（「收徒」按钮旁），徒弟侧不再提供解除按钮。
- **外门弟子来投概率下调**：每旬掷一次，基础 2%，每 10000 名庇护人数 +1%，总概率封顶 5%（原为 2% + 庇护/20000、封顶 14%）。
- **功德台调整**：灵泉 5%→2%、神兽苑 3%→2%、藏经楼 6%→2%；灵泉 / 神兽苑 / 灵雨台 / 聚宝盆 / 异火窟 / 万物鼎 / 地图室 / 鉴宝堂等级上限 50，冥思阁上限 30。
- **择师尊页**：优胜者卡片不再显示内部评分，改显示灵根文案（如「金木水四灵根」）。

### 修复

- **任命 / 选人弹窗重建后点行卡死**：弹窗打开期间发生一次重建后，旧卡片节点被释放而引用残留，点行时遍历到已释放对象导致「Trying to cast a freed object」刷屏卡死。现重建时先清空引用，并对残留引用做有效性防护。
- **逐出宗门牵连文案称呼写反**：现按被逐者视角正确记录（其师尊记「徒弟」、其徒弟记「师尊」、双亲记「子/女」、子女记「父/母」）。
- **宗门大比择奖品法宝加成文案溢出卡片**：加成文案改自动换行并加高卡片，长文案完整显示在框内。
- **确认弹窗长文案挤掉按钮**：弹窗高度改按文案行数自适应加高，底部「确定 / 取消」不再被挤出边框。

### 界面

- **通知条改为宣纸质感卡片**：飘字通知由纯黑底改为纸底 + 墨字 + 淡描边 + 投影，长文案自动换行居中，与整体水墨 UI 统一。
- 行动页悬浮按钮「回到最新」改为「最新」。

### 优化

- `index.pck` 由 7.49 MB 变为 **7.52 MB**（+约 29 KB）：本轮含多项新功能与交互，无新增美术素材。

---

## [1.1.2] - 2026-09-18

### 修复

- **门徒详情弹窗「点窗外误触进他人详情」**：原遮罩在按下瞬间即关闭窗口，抬手时手势穿透到下方门徒列表、直接打开了相邻角色的详情；即便不穿透，「点窗外」本应表示「取消当前窗口」而非「切换查看别人」。
  现改为：① 遮罩仅在**抬手**时关闭窗口；② 关闭后 300ms 内屏蔽详情派发，吞掉关窗瞬间的误触；③ 再次点击某角色才查看他的详情。彻底消除「一摸就串门」。

### 界面

- **宗门大比弹窗 UI 优化**：择奖品 / 择师尊两步重做——
  - 顶部新增**步骤指示条**（选奖品 → 选师尊），当前步高亮；
  - 奖品 / 师尊卡片改为**整卡点选高亮**：选中金边 + 「已选」标签，不再依赖「点击选择」小按钮（同时修掉一处隐蔽样式 bug：`Button.flat=true` 会吞掉 normal 态底色、选中金底不绘制，已去掉 flat、改自绘选中态）；
  - 择师尊后展示**夺魁战报卡**（灵根球 + 境界 / 武力 / 评分一目了然）；
  - 候选内门以**带灵根球的小行**列出，点选即高亮；
  - 去掉「返回」按钮（流程可随时取消，无需回退）。
- 大比弹窗由 800 宽，候选列表加右边距避免滚动裁切。

### 优化

- `index.pck` 由 7.48 MB 变为 **7.49 MB**（+约 10 KB）：本轮全部为界面与交互手感修复，无新增素材。

---

## [1.1.1] - 2026-09-18

### 调整

- **杂役每人「合计」只给 +1% 效率**：此前除「+1% 基础劳动」外还有一条「职务效率 ×(1 + 5% × 在岗人数)」乘子，
  单峰塞满 100 人时职务部分会被放大到 6 倍，与「每个杂役固定提供 1%」的设计不符。
  现把该乘子一并改为 **1%/人**（`MENIAL_BONUS` 0.05 → 0.01），杂役对产出的全部贡献即为每人 +1%。
- **老存档兼容**：旧版杂役上限为 1200，新版全宗上限 300；读档后直接硬夹到 300
  （原逻辑每旬只 -1，要数百旬才降下来）。

### 修复

- **宗门大比弹窗里点「选择」没反应**：弹窗内多个回调只置了 `_force` 却没置 `_dirty`，
  而重建门槛是 `_dirty and _can_rebuild()` —— 少了置脏就不会重绘，界面看起来像点不动。
  该分支此前还被测试用例「直接调用处理函数」绕过，所以回归是绿的；现已补置脏，并让用例改点真实按钮。
- **大比到点后整个模拟被冻结**：`tick()` 开头对挂起态直接 `return`，时间彻底停摆。
  现改为挂起期间**时间照常推进**，顶栏「宗门大比」按钮一直挂着 0 旬，直到玩家点开并主持完大比为止。

### 界面

- 行动页「回到最新」由「内容末尾的按钮」改为**常驻悬浮按钮**（钉在右侧、虚拟滚轮下方）：
  滚到任何位置都可见，离开行动页自动隐藏。门徒名册的「回到顶部」维持原样。

### 优化

- `index.pck` 由 7.48 MB 变为 **7.48 MB**（+2 KB）：本轮为数值与逻辑改动，无新增美术素材。

---

## [1.1.0] - 2026-09-18

游戏正式更名为「我的掌上仙宗」；生产体系与宗门大比全面重做的一版。

### 新增

- **游戏正式更名为「我的掌上仙宗」**：工程名、浏览器标题与文档同步更新。网页存档挂在 `/userfs`（与工程名无关），老玩家进度不受影响。
- **青芜峰 / 玄矿峰改为品阶选择列表**：取消原先的「30 级开垦」逐级递进，改为与镇邪峰、寻幽峰同款的下拉选择——直接指定要种哪一阶灵草、采哪一阶灵矿。基础效率：荒阶 **1 个月** / 株（块），穹极阶 **10 年**，中间各档线性平滑（昊苍阶约 11.2 年）。
- **宗门大比改为玩家主持**：不再到点自动结算。

### 调整

- **丹宸峰 / 玄铸峰改为「按件计时」**：成一件扣 **2 个对应品阶材料**（原为每旬扣 2 灵草 / 3 灵矿）。
  - 炼丹基础效率：还春丹 **半年**、筑基丹 **1 年**、通天丹 **100 年**、洗髓丹 **120 年**，中间各档线性平滑。
  - 铸造基础效率：荒阶 **半年**、昊苍阶 **100 年**，中间各档线性平滑。
  - 品阶越高不再「炼得越慢」，难点回到攒料本身。
- **宗门大比流程重做**：到点后顶栏「宗门大比」凸起成按钮并挂着 0 旬，玩家点击才开流程 —— ① 从**公库**择一件法宝作为奖品；② 外门弟子按「境界 + 最大武力」加权取优胜者、晋升内门；③ 可择一位内门弟子拜为师尊，也可选择不拜。任一步取消则按钮继续挂 0 旬等待。
- **杂役大幅削弱**：每人由「相当于 1 个满效率劳动力」降为 **+1% 基础劳动**；上限收紧为单峰 100 人、全宗 300 人（原 200 / 1200）。
- **门徒上限调整为 206 人**：屋舍最高等级改为 40 级（`6 + 40 × 5 = 206`）。
- **突破率丹药限次**：同一角色每次冲关，同种丹药最多服用 **2 颗**（冲关失败后刷新次数）；提前服用更高境界的丹药不再生效，也不消耗。加成记在独立字段上，冲关成功或失败后清零。

### 界面

- 灵根页底部新增「可用功德」，样式同天赋页。
- 设置页进入新轮回前，提示本次轮回预计结算功德。
- 弹窗滚动回正全部关闭，可任意悬停停留。
- 行动页：宗门编年底部加透明「回到最新」，门徒名册底部加透明「回到顶部」。
- 角色详情：法宝栏未穿戴的栏位新增「穿戴」按钮，弹窗列出公库中对应部位的法宝。
- 拜师 / 收徒 / 服药 / 赐予法宝等一切「点击选择门徒」的弹窗，统一改为**选中后需点「确定」**才生效，避免误触。

### 修复

- 大比挂起时原逻辑会 `return` 跳过当旬的投奔、推举等结算，现改为不中断时间推进。

### 优化

- `index.pck` 由 7.46 MB 变为 **7.48 MB**（+0.02 MB）。

---

## [1.0.10] - 2026-09-17

数值打磨与内容扩充的一版：功法强度与撰写人造诣挂钩，堕魔节奏放缓，新增 50 条行动事件。

### 新增

- **行动事件扩充至 124 条**（原 74 条）：按维度全覆盖——修炼/突破/心境、战斗/武力、探索/奇遇、资源正负、社交/宗门、日常闲适、情缘、亲缘补强，各维权重沿用既有区间，数值事件统一走 `eff` 结构化施加、文案写死数值。

### 调整

- **功法单卷强度与「撰写人造诣」挂钩**：六类效率书 = `base × 撰写人该类别基础效率`（`base_eff` 剥离法宝/功法/心情/灵石，只取固有永久加成——灵根品质、属性匹配、出身、特质、功德建筑、师徒、事件累积）；造诣越高书越强。突破率书 = `base × 20%`、渡劫死亡率书 = `−base × 10%`（取负即降死亡率）。
- **堕魔节奏放缓**：宗门级改为全宗每旬 0.1% 掷一次骰、命中随机抽 1 名弟子堕魔（≈27.8 年/宗，较旧口径慢约 5.6 倍）；杀妻·杀夫证道 0.5%/旬、身存魔种 0.6%/旬（不累计、纯概率）；修魔创建即视为已堕魔。
- **死亡类功法统一取负**：旧实现给正值（=每卷抬高死亡率、实为负面），现改为负值（降低死亡率），消费端显示亦按符号着色，不再出现「死亡 +14%」误读。
- **「寒潭灵脉」收益下调**：修为加成由 +30% 改为 **+10%**。

### 修复

- **已堕魔者不再弹出堕魔事件**：原「无法突破心魔，堕为魔修，从此再无回头路」对已堕魔者本就无实际惩罚（有 `demonDone` 守卫不重复扣突破率），但仍会记一句突兀红字；现已在事件建池时直接跳过，连文字都不出现。

### 优化

- `index.pck` 由 7.35 MB 变为 **7.46 MB**（+0.11 MB）：本轮全部为数值、逻辑与事件数据改动，无新增美术素材（新增 50 条事件 + 3 个测试探针随包打入）。

---

## [1.0.9] - 2026-09-16

数值与风险的一版：修炼节奏全面重做，高境界破境改为硬扛九道天劫，飞升终于成了稀罕事。

### 新增

- **三道天劫**：合体圆满破渡劫期为**九天雷劫**、渡劫期圆满破大乘期为**破妄灵劫**、大乘圆满飞升为**飞升玄劫**。进入这三个境界的大圆满之期后，每一年（36 旬）降下一道劫，共 9 道；**每道都是一次生死判定——过则续下一道，不过则身死道消**。九道全过方可破境，境界名后会直接显示进度（如「大乘大圆满 · 玄劫 3/9」）。
- **天劫加成体系**：丹药与各类突破加成直接作用于「总通过率」，再均摊到 9 道劫上，因此吃丹药、堆突破率对渡劫同样有效。
- **重生特质重做**：原「免疫渡劫陨落」改为**三层前世残魂替死**——致命时消耗一层，**该道劫算作已过、直接续判下一道**；三层用尽后再致命即身死。

### 调整

- **修炼曲线全面重平衡**：每小阶所需旬数改由 `CULT_BASE` / `CULT_GROW` 决定（练气境速度与旧版持平，此后逐境匀速变陡），境界寿元表改为 **80 / 200 / 500 / 800 / 1200 / 1800 / 2500 / 3500 / 5000**。灵根品质因此直接决定能否飞升：裸修下变异天灵根约 2000 年飞升、天灵根约 2900 年、变异灵根约 3900 年、单灵根约 4500 年，双灵根及以下基本止步于坐化。
- **冲关开始有代价**：练气～分神的大圆满冲关失败时，**灵力溃散一成进度条**，须重新修满方可再冲（小境界仍是攒满自动晋升，不受影响）。
- **洗孽池上限 50 级**：天劫之期改按「每级 +0.25 个百分点」提升**每一道劫**的通过率；50 级另有功德建筑总上限。
- **身负龙气与装备 / 功法的渡劫属性**：在天劫之期改挂在「总通过率」上（原描述文案不变）。
- **有职务者修炼惩罚放宽**：掌门 ×0.8、其它职务 ×0.9（原 0.5 / 0.7 在新曲线下等于「谁当掌门谁坐化」）。

### 界面

- 门徒名册卡片与弟子详情页在**境界处显示天劫进度**（如「大乘大圆满 · 玄劫 3/9」），一眼看出已过几道。
- 弟子详情页进入天劫之期后，原「渡劫死亡率」一栏改为**「单道雷劫通过 X%」**——旧字段在这套规则下已不再适用。
- 功德建筑满级时显示 `lv50/50 · 已满级`。

### 修复

- **重生特质的描述与实际不符**：原文案「并有可能在亲友遇到随机死亡事件时拯救他们」在代码中从未实现，现已按新效果改写。
- **界面上的「突破几率」此前名不副实**：旧口径下大圆满每旬只掷 1/8 的概率，显示的百分比并非单次成功率；现在大圆满会先修满灵力、再按界面所示概率判定一次，名实相符。

### 优化

- `index.pck` 由 7.33 MB 变为 **7.35 MB**（+0.02 MB）：本轮全部是数值与逻辑代码改动，无新增素材。
- 顺带修掉一处导出漏排：开发期界面截图目录 `_shots/` 此前未列入导出排除项，现已加入，不会再被打进发布包。

---

## [1.0.8] - 2026-09-16

界面与手感的一版：全屏铺满不留黑边，名册与详情页一并重做，顶栏不再「贴」在顶上。

### 新增

- **门徒名册可筛可排**：新增筛选条（全部 / 在册 / 已飞升 / 已陨落 / 休眠）与排序条（境界 / 战力 / 年龄）；卡片重做——灵根文案旁紧跟迷你灵根球，性别·年龄右置，灵力增长条居中，底部一行「武力 / 突破 / 灵力」三段数值，卡片改暖金描边。
- **弟子详情页鎏金美化**：页签改「角色 / 经历」鎏金胶囊，各小节加左侧金条，修为格卡按语义着色（修炼效率与武力鎏金、渡劫死亡率朱红），心情条圆角化，关系操作按钮改金边。排版沿用原结构，不改动信息顺序。
- **顶栏渐隐融合**：顶栏去掉四边硬描边，下方加一条由纸色渐变到透明的过渡带；资源格去掉描边、改淡墨底色（告警时转淡朱），不再像一张贴在顶上的表格。
- **全出血布局**：`stretch/aspect` 改为 expand，视口随屏幕比例铺满，长屏手机不再出现上下黑边；安全区收紧为 上 56 / 下 72 / 左右 28，一屏能多摆几张卡片（弹窗高度另行封顶，长屏观感保持一致）。
- **状态栏真实读数**（Web 版）：经 JS 桥读取设备电池百分比与充电状态、网络类型；电池充电染翠绿、低于 20% 染朱红。读不到的设备（或原生端）自动退回静态图标，不会显示错误数字。
- **特质五分类着色**：负面=朱砂红、普通=墨灰、正面=青竹绿、特殊=暗鎏金、修魔=黛紫；名册卡片特质 chip 与详情页「特质」行同步着色。

### 调整

- **建立宗门整页不再显示滚动条**：三步向导内容本就接近一屏，右侧那根灰色竖杠已彻底隐藏（手指仍可正常拖动）；开宗立派后恢复正常滚动条。
- **行动页普通行动正文改为墨灰**：金色只留给吉凶语义与时间轴节点，大段正文回到墨色，长列表读起来更稳。

### 修复

- **暂停 / 推进按钮文案与实际状态不同步**：暂停后按钮仍显示「暂停」，需等下一次推进才翻转；现点击即时更新。
- **堕魔事件未按坏消息着色**：「无法突破心魔，堕为魔修」此前落成墨灰，现与其它凶事一致，时间轴红节点、正文红字。

### 优化

- `index.pck` 由 7.32 MB 变为 **7.33 MB**（+0.01 MB）：本轮全部是界面与逻辑代码改动，无新增素材。

---

## [1.0.7] - 2026-09-16

建立宗门体验重做：三步向导分步展开，灵根品质一眼看懂，修炼效率与武力按属性细分。

### 新增

- **建立宗门改为「三步向导」**：壹身世（姓名 / 性别 / 出身）、贰灵根（转动圆球重测灵根并实时预览修炼效率与武力）、叁天赋（功德建筑 / 特质），分步展开、底部独立固定条结算功德后开宗。
- **灵根品质示意图**：建立宗门页新增四颗对照球——**天灵根** / **变异灵根** / **特殊灵根**（冰·风·雷三色）/ **普通灵根**（金·木·水·火·土五色），一眼看懂灵根品质档位。
- **灵根球下方双效率条**：贰灵根步骤的「修炼效率」与「武力」改为自绘鎏金进度条，满档按细分后的理论极值归一（风单根修炼 3.96 / 雷单根武力 1.65），数值高低一目了然。

### 调整

- **建立宗门特质顺序重排**：需要功德的特质按「+功德最前 → 不耗功德 → 需功德由低到高」排列；首局功德为 0（轮回起点）也能从容挑选。
- **灵根修炼效率与武力按属性细分（平均口径）**：同档品质内，修炼效率与武力随「金木水火土冰风雷」属性不同而微调（±10% 带宽）；多灵根取根内各属性系数的算术平均，不再区分主战 / 主修。八档品质共 67 种组合全部接入，全游戏弟子、长老、NPC 的修炼与生产自动带上细分加成。

### 修复

- **建立宗门贰灵根页效率条此前不渲染**：原先用锚点 + 子控件搭的条形图在实机不出图，已改为自绘，并加断言兜底。

### 优化

- `index.pck` 由 7.64 MB 变为 **7.67 MB**（+0.03 MB）：本轮全部是逻辑与界面代码改动（新增 EffBar.gd 自绘效率条、灵根属性细分系数），无新增素材。

---

## [1.0.6] - 2026-09-15

界面手感的一版：行动页改成编年史书，手指在屏幕上哪儿都能滑。

### 新增

- **行动页改为「宗门编年」史书时间轴**：原来的纪事卡片流水换成一条竖向时间轴——
  **年份**是金色方菱大节点（一眼看清编年分段），**每一旬**是按吉凶语义着色的小圆点，
  行内只留日期与正文。同屏密度比旧版高约 16%，且多出年份锚点，长列表不再「一片糊」。
- **正文跟着吉凶一起变色**：编年条目与弟子「经历」页的正文颜色改用与节点对应的**淡一档语义色**
  （吉偏绿、凶偏红、平偏蓝、突破偏金）——既保留了「一眼看出好坏」，又比节点色浅，正文长时间读不刺眼。
  「经历」页原先墨 / 金 / 红 / 绿各写一份且只有突破用金，现已统一到同一套配色。

### 调整

- **手指滑动改为全屏生效**：以前只有卡片之间的窄缝能拖动——因为卡片必须拦住触摸才能点击，
  拖动事件传到卡片就被截断了。现在手势在更早的广播层接管，**按在任意卡片、面板、甚至顶栏上都能直接滑动**，
  不再需要特意去找空白处；摇杆与滚动条区域仍独占手感，不会互相抢。
- **编年条目留白收紧**：条目底部留白 20 → 14px、年份行上下 8 → 6px，内容区顶部与底部同步收一档，
  单条纵向占用 114 → **108px**，年份行 → 71px。
- **弟子详情「经历」页显示区域放大**：原先列表高度被写死成 420px，一张纸大半空着、一屏只见 3 条；
  现在撑满弹窗可用高度，可视高度 420 → **1251px**（占弹窗 77%），一屏 3 → **11 条**，仍可继续滚动。
- **建立宗门页文案**：「转动 xxx 灵根」改为「**转动圆球重测灵根**」。

### 修复

- **建立宗门页灵根区的一块黑色矩形**：那块是「名字 + 重测提示」的点击热区，
  因为漏了覆盖面板样式盒，退回了引擎默认主题（深灰、直角、无描边），压在墨玉色卡片上就是一块更黑的方块。
  现已覆盖为透明，同时加了全树扫描断言，杜绝同类遗漏。
- **时间轴方菱节点的包围盒**：圆角会削掉四角，若按「边长 × √2」算包围盒，轴线断口会被撑宽约 18%
  （节点上下多出一截空白）；已按圆角修正。

### 优化

- `index.pck` 由 7.63 MB 变为 **7.64 MB**（+0.01 MB）：本轮全部是逻辑与界面代码改动，无新增素材。

---

## [1.0.5] - 2026-09-15

移动端手感与规则收口：不再误触、不再回弹，灵根组合立了规矩。

### 新增

- **灵根「分组」规则**：金木水火土为**五行组**、冰风雷为**异属组**；**两个以上灵根只会在同一组内组合**，
  不再出现「金冰」这类跨组拼盘。多灵根先抽组（五行 70% / 异属 30%，异属更稀有），
  单灵根不受限（八种属性都能单独成根），四灵根以上异属组凑不齐、只会落在五行组。
  子嗣继承同样遵循：优先取父母并集的同组属性，缺口在同组内补齐。
- **触摸手势闸门**：所有「摸一下就跳页」的位置统一改为按下时记录坐标与时间、抬手时才判定——
  **位移超过 8px 判为滑动滚动、按压超过 300ms 判为长按，两者都不触发跳转**；
  只有「位移 < 8px 且时长 < 300ms」才算一次点击。
  门徒列表卡片、底部页签、建立宗门的出身卡与特质卡、灵根转动球均已接入。
- **可滑动范围指示**：所有可滑动区域常显一条细指示条，条长即「当前可见部分占全文的比例」，
  一眼能看出这块还能滑、以及还剩多少。

### 调整

- **右侧虚拟滚轮改为「上下陀螺仪」**：不再是 ▲ / ▼ 两个按钮，而是一根竖轨 + 一个可推的圆钮——
  往上推持续上滚、往下推持续下滚、推得越远越快，松手圆钮回中即停。尺寸、配色、透明度与旧版一致，位置略微下移。
- **青芜峰移到丹宸峰与玄矿峰之间**（设施页六峰顺序）。
- **宗门设施行操作右置**：外门 / 杂役 / 开垦改为右对齐的 `[−] 数值 [+]` 步进器，加减按钮加宽；
  副手由左侧「+」改为右侧「选择」，峰主按钮改为「任命」，各行按钮右边界对齐成同一列。
- **顶栏整体压缩约 135px**：内外边距、时钟、宗门名与资源格字号同步收一档，把纵向空间还给内容。
- **弹窗四角统一为同一圆角**，去掉底部原先的特殊处理。

### 修复

- **修复滚动位置「自动回正」**：界面每 400ms 会重建一次，重建瞬间内容高度归零、滚动条被钳回顶部。
  现在重建前后会记住并接回每个滚动区域的当前位置。
- **修复下拉框「点开就回弹关掉」**：同一次重建会把持有下拉菜单的控件一起销毁，菜单刚展开就被干掉；
  现在下拉菜单打开期间暂停重建。
- **修复顶栏四角漏出灰黑底色、弹窗正文被斜边切断**：自绘样式引擎在画直角时漏掉角点，
  相邻两条边各少画一段，多边形把整个直角连同一大块斜切成了斜边。
- **修复右侧滚轮「推了不动」**：目标位置按帧取整会把不足 1px 的增量全部截掉，高帧率下速度被整段吃光。

### 优化

- `index.pck` 由 7.61 MB 变为 **7.63 MB**（+0.02 MB）：本轮全部是逻辑与界面代码改动，无新增素材。

---

## [1.0.4] - 2026-09-15

灵根球视觉与「宣纸卷轴」界面质感。

### 新增

- **灵根球视觉**：灵根不再只是一个名字，而是一颗球——单灵根为纯色宝球，双 / 三 / 五灵根按扇区均分颜色，
  变异灵根叠异色辉光，天灵根叠金色圣光。建立宗门界面的灵根卡与门徒详情 / 名册统一使用；
  点击「转动灵根」时球体与辉光一起换新。
- **「宣纸卷轴」界面质感体系**：新增自绘样式引擎（`InkStyle`），纸面自带
  **纵向微渐变 + 顶部内高光 + 内描边 + 双层投影 + 内凹槽**，取代原先的纯色平涂。
  表现规则统一为：**能点的浮起来、只读的凹下去**。
- **国风母题**：顶栏朱砂印章、云纹分隔线、云勾角花、资源格金圆牌、页签改「卷签」，
  背景铺一层宣纸噪点。
- **纪事卡左缘状态色条**：按事件类型着色（机缘金 / 增益绿 / 受阻红 / 常规蓝），一眼分辨纪事性质。

### 调整

- **全局圆角统一放大一档**，纸面观感更圆润，与设计稿一致。
- **按钮按下手感**：底部 3px「唇」与投影在按下时一起收掉，按压反馈更明确。
- **输入框与进度条底槽改内凹材质**，与浮起的纸面形成层次。
- **建立宗门出身卡图标**由 emoji 换为国风字形，与其余图标风格统一。

### 修复

- 修复「转动灵根」按钮的卷轴图标始终空白的问题——白色版素材 `icons/w/lg_roll.png` 此前并不存在，
  运行时染色取图自然取不到，现已补齐。
- 修复建立宗门出身卡图标在子集字体下渲染成方块的问题（emoji 字形不在子集内）。

### 优化

- `index.pck` 由 4.93 MB 变为 **7.61 MB**：新增的灵根球图集（约 140 张，覆盖全部灵根组合）占其中 2.5 MB。
  已核对导出过滤器工作正常，未混入本地备份目录与开发截图。

---

## [1.0.3] - 2026-09-15

字体与图标体系全面接入，界面与视觉设计稿对齐。

### 新增

- **图标素材体系（30 枚）**：8 个峰图标、8 个资源图标、5 个道具分类、5 个底部 Tab、灵根转动 / 存档 / 状态信号等，
  全部以 512×512 PNG 导入工程；单色图标另生成白色版，运行时按需染色（金 / 灰 / 红），替代此前的文字方块占位。
- **状态栏**：顶部新增真实时钟与静态状态图标。
- **弹窗把手**：底部弹窗加拖拽把手与顶部大圆角，卡片式观感更统一。

### 调整

- **字体换为思源宋体 / 思源黑体**（对齐设计稿）：
  - 正文与标题使用 **Noto Serif SC**（思源宋体），数字、资源格数值、胶囊标签、时间使用 **Noto Sans SC**（思源黑体），
    与此前 12px 点阵字体相比可读性显著提升，也终于与设计稿的字形一致；
  - 两款字体均为 SIL OFL 1.1 协议，可自由嵌入与分发；
  - 字体按 **GB2312 6763 字 + 项目实际用字**做子集化后嵌入（约 4.7 MB），不再依赖运行期联网加载字体，
    Web 端不会因为字体加载失败而回退成系统字体。
- **八峰配色与峰图标**：补齐丹宸 / 玄矿 / 撰书 / 玄元四峰的专属色，设施页每峰配专属彩图标与浅底色块。
- **顶栏资源区**：由胶囊行改为 **4×2 资源网格**（图标 + 名称 + 数值），庇护人数为 0 时整格转红警示。
- **底部导航**：改为「图标 + 文字」竖排，选中态为金色并显示顶部指示条。
- **道具页**：灵草 / 灵矿 / 丹药 / 法宝 / 功法五类分类头各配专属图标。

### 优化

- **Web 包体瘦身**：导出时排除已不再使用的点阵字体与本地备份目录，
  `index.pck` 由 5.90 MB 降至 **4.93 MB**（在新增 4 套字体的前提下反而更小）。

---

## [1.0.2] - 2026-09-14

移动端适配：输入、滚动、字号三大问题集中优化。

### 新增

- **Web 端姓名/宗派名输入兜底**：在微信等手机浏览器中，Godot 的 `LineEdit` 无法调出虚拟键盘。
  现在 Web 环境下建宗界面显示「输入按钮」，点击调用浏览器原生 `prompt` 输入，非 Web 环境仍使用原生输入框。
- **全局滚动支持手指拖动**：新增 `MobileScrollContainer`，为所有 `ScrollContainer` 补上
  触摸（及鼠标）拖拽滚动；页面过长时手机可正常上下滑动。

### 调整

- **视口基准改为 720×1280**：原来 1080×1920 在手机上被缩得太小，降低基准分辨率后 UI 整体放大、更容易看清。
- **字号与间距整体放大一档**：正文/小字/标题全面加大，按钮、面板、卡片内边距同步增加；
  一行放不下时通过 `wrap_label` / `HFlowContainer` 自动折行，不追求单列挤在一行。
- **选人弹窗、大比奖品弹窗的列宽改为弹性**：固定列宽改为「可扩展」，避免字号放大后文字被截断。

### 修复

- 修复手机上打开 Web 版不能用手上下滑动的问题。
- 修复「建立宗门」弹窗在手机上**超出屏幕宽度、底部被截断且无法滑动**的问题（按 6.3 寸 / 390 逻辑宽严格控制，大屏等比例自适应）：
  - 弹窗外框宽度**锁死在可视区以内**（`frame-16`），高度也**封顶在屏幕内**，超长内容在弹窗内纵向滚动，不再被截断或拉出屏幕；
  - 关键根因：弹窗内部 `ScrollContainer` 的最小宽度等于内容最小宽，长文案 / 特质按钮会把它硬撑宽、进而把整块顶出屏幕。
    现改为滚动容器宽度作为**硬上限**覆盖内容最小宽，所有长描述（`灵根`、`特质` 等）改 `wrap_label` 自动折行，绝不反向撑大；
  - 特质按钮由「固定最小宽」改为 **2 列 `GridContainer` 等分填满**，窄屏自动换行、永不出框。「提示」「空名册」等短弹窗也改为自适应高度。

### 修订（同版本覆盖发布）

全流程界面重排（仅调整 UI 排布，数值、玩法逻辑与术语不变）：

- **整体视觉**：改用水墨卡片风格——纪事条目改为带色条（吉/凶/常）的独立卡片；设施页七峰改为「图标块 + 峰名 + slogan」卡头；
  门徒卡改为「圆形头像 + 姓名 + 境界胶囊 + 职务右对齐 + 迷你进度条 + 特质胶囊」；道具页分类加图标头与计数胶囊；
  设置页功德建筑改 2 列格卡、天梯加序号圆点；底部导航改为等宽页签，顶部资源统计改胶囊样式。
- **建立宗门（捏人）**：居中题字与金线分隔，性别改分段按钮，灵根与出身改卡片选择，可用功德条与「开宗立派」全宽按钮。
- **弟子详情**：属性改为分节 + 2 列属性格卡，心情用进度条呈现。
- **选人弹窗**：宽度放宽至安全区内最大值（920），**高度改为按候选人数自适应**（此前固定撑满整屏、下方留大片空白）；
  列表文字改小号并重新按内容配比列宽——境界（如「出窍四阶」）、职务（如「青芜峰峰主」）、长灵根名（如「火木雷风四灵根」）不再被截断。

---

## [1.0.1] - 2026-09-14

战斗与损耗系统上线，经历记事着色，界面打磨。

### 新增

- **经历着色**：弟子「经历」中好事件着绿色、坏事件着红色、突破/晋阶着金色，其余默认墨色，纪事一目了然
- **武力「当前/最大」显示**：人物详情与门徒卡片展示 `当前武力 / 最大武力`；
  镇魔峰、寻幽峰的攻略判定均以**当前武力**为准（损耗后实时反映）
- **战斗损耗系统**：除魔地、秘境的攻略结果会影响参战成员的当前武力
  - 轻松获胜：无损
  - 普通通过：损失 30% 当前武力（记为「损耗」）
  - 讨伐失败：损失 70% 当前武力
  - 中间档位按风险平滑过渡（小有损伤 30% ~ 70%）
- **损耗自然恢复**：当前武力的损耗随时间缓慢恢复，约 1 年（36 旬）恢复满额
- **「伤势」概念**：暂时性的最大武力恢复上限折损，**不体现在最大武力数值上**；
  有伤势时损耗恢复只能回到 `（1 − 伤势）× 最大武力`。详情页在 `伤势 > 0` 时追加「伤势 N%」行
- **还春丹改治伤势**：每颗「还春丹」消解 10% 伤势，并令当前武力的恢复上限回升

### 调整

- **镇魔峰 / 寻幽峰战役改为「每年结算一次」**：攻略进度仍每旬平滑累积，但真正的战役结算
  （结果判定、损耗、伤势、纪事）只在每年首旬进行一次；修复了此前每旬都结算、纪事刷屏的问题
- **门徒页**：移除门徒名册下方的说明文案，界面更清爽

### 修复

- 秘境攻略失败时「损失寿命」事件现在额外附加 30% 伤势（此前只扣寿命、不记伤势）

---

## [1.0.0] - 2026-09-14

首个公开版本，发布 Web 版。

### 新增

- **弟子系统**：十大境界 × 十阶（练气 → 飞升）、灵根与五行、寿元与突破失败/陨落判定
- **七峰经营**：镇邪峰、寻幽峰、青芜峰、玄矿峰、丹宸峰、玄铸峰、撰书阁
- **人员编制与效率**
  - 峰主 100% / 副手 50% / 外门弟子 20% / 杂役弟子每人 +5%
  - 杂役弟子总人数上限 1200，单峰上限 200
- **产出门槛**：四座资源峰未达门槛时不产生收益
  - 青芜峰 / 玄矿峰：杂役在岗 ≥1 或 外门在岗 ≥1 或 有副手 或 有峰主
  - 丹宸峰 / 玄铸峰：外门在岗 ≥1 或 有副手 或 有峰主
- **事件系统**：弟子奇遇随机事件，物品得失品阶按境界匹配（避免低阶弟子获得高阶物品）
- **功德建筑**：12 座，跨轮回永久保留
- **法宝装备**：五槽位、十品阶
- **轮回传承**：弟子寿终转世，宗门积累延续
- **Web 导出**：无线程 WebAssembly 构建，可直接静态托管（GitHub Pages）

### 修复

- 修正 `_apply_eff` 中 `match` 分支缩进与括号导致的脚本解析错误
- 补齐 `project.godot` 中缺失的 `XiuxianGlobals` 自动加载注册
- 修正导出时未排除 A/B 测试数据目录（约 25 MB）导致包体膨胀

### 技术说明

- 引擎 Godot 4.7.2，渲染后端 GL Compatibility
- 导出使用无线程模板（`GODOT_THREADS_ENABLED = false`），
  因此无需服务器提供 COOP/COEP 响应头，兼容任意静态托管平台
